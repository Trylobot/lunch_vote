
> sudo -u ubuntu ssh -i ~/.ssh/github_deploy_id

> sudo nano /usr/bin/update_app.sh
#!/bin/bash
cd /home/ubuntu/misc/lunch_vote_js
sudo -u ubuntu git fetch --all
sudo -u ubuntu git reset --hard origin/master
sudo npm install
sudo forever stopall
sudo NODE_ENV=production forever start app.js

> sudo nano /etc/rc.local
sudo /usr/bin/update_app.sh

